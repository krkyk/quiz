<header>
  <nav class="navbar navbar-expand-lg">
    <div class="container">
      <a class="navbar-brand" href="/"><%= image_tag"prime.png",size: "250x75" %></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon">
            <span></span>
            <span></span>
            <span></span>
          </span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
          <ul class="navbar-nav ml-auto">
            <% if user_signed_in? %>
              <li class="nav-item">
                <%= link_to(new_prime_path, class: "nav-link btn btn-light mx-3") do %><i class="fas fa-star"></i> 検索<% end %>
              </li>
            <% elsif admin_signed_in? %>
              <li class="nav-item">
                <%= link_to(admin_blogs_path, class: "nav-link btn btn-light mx-3") do %><i class="fas fa-star"></i> ブログ一覧<% end %>
              </li>
              <li class="nav-item">
                <%= link_to(new_admin_blog_path, class: "nav-link btn btn-light mx-3") do %><i class="fas fa-star"></i> ブログ投稿<% end %>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to(about_path, class: "nav-link btn btn-light mx-2") do %><i class="fas fa-star"></i> 使い方<% end %>
              </li>
            <% end %>
          </ul>
        </div>
    </div>
  </nav>
</header>